# Quiz — ft_rev_int_tab

> **Topic-specific questions** · array reversal  
> Discuss reasoning in meeting — logic beats memorization.

**Answer key:** [bottom](#answer-key)

---

## Multiple Choice (10)

### MCQ 1
What is the main learning goal of **ft_rev_int_tab**?

- A) Copy paste from GitHub  
- B) Understand **array reversal** in memory  
- C) Skip testing  
- D) Use only `printf`  

### MCQ 2
Before writing `ft_rev_int_tab`, you should:

- A) Start typing C immediately  
- B) Draw memory and write pseudo-code  
- C) Disable warnings  
- D) Submit without compiling  

### MCQ 3
Which flag set matches 42 norm?

- A) `-w`  
- B) `-Wall -Wextra -Werror`  
- C) no flags  
- D) `-O3` only  

### MCQ 4
A segmentation fault usually means:

- A) Invalid memory access  
- B) Missing semicolon always  
- C) File not found  
- D) CPU overheating  

### MCQ 5
Related concepts for this exercise include:

- A) array reversal  
- B) HTML/CSS  
- C) Docker only  
- D) None of the above  

### MCQ 6
Best first step when stuck:

- A) Random code changes  
- B) Trace by hand on paper  
- C) Delete the function  
- D) Turn off compiler  

### MCQ 7
`source.c` in Poolers is for:

- A) Moulinette submission  
- B) Study and live demos — write your own for submit  
- C) Copy into intra  
- D) Replace the subject  

### MCQ 8
Off-by-one errors often come from:

- A) Wrong loop start/end  
- B) Wrong OS  
- C) Too many comments  
- D) Using `write`  

### MCQ 9
Return type of `ft_rev_int_tab` must match:

- A) The 42 subject prototype  
- B) Whatever you prefer  
- C) Always `float`  
- D) Always `void main`  

### MCQ 10
Understanding beats memorizing because:

- A) Exam exercises vary  
- B) You can derive solutions under pressure  
- C) Both A and B  
- D) Neither  

---

## True or False (10)

1. You should read **LESSON.md** before **source.c**. **T / F**  
2. Compilers catch all logic bugs. **T / F**  
3. Every variable has an address in memory. **T / F**  
4. ASCII maps characters to numbers. **T / F**  
5. Copying Poolers `source.c` to intra is allowed. **T / F**  
6. `-Werror` treats warnings as errors. **T / F**  
7. Tracing helps before running GDB. **T / F**  
8. Edge cases matter for moulinette. **T / F**  
9. **array reversal** is relevant here. **T / F**  
10. Peer explanation solidifies learning. **T / F**  

---

## Coding (5)

1. Write the prototype for `ft_rev_int_tab` from memory.  
2. Implement smallest case only (no loops if possible).  
3. Add full subject behavior.  
4. Add comments explaining memory per line.  
5. Write a `main` that tests 3 cases.  

---

## Memory (5)

1. Draw stack before `ft_rev_int_tab` runs.  
2. Draw stack inside `ft_rev_int_tab` at mid-execution.  
3. Label each local variable's type and size.  
4. Show stdout buffer after each output line.  
5. What changes when the function returns?  

---

## Debugging (5)

1. Symptom: extra character printed — one cause?  
2. Symptom: infinite loop — one cause?  
3. Symptom: compile error on prototype — one cause?  
4. Symptom: no output — one cause?  
5. How would you use `gcc -Wall -Wextra -Werror` here?  

---

## Answer Key

| Section | Hint |
|---------|------|
| MCQ | 1B 2B 3B 4A 5A 6B 7B 8A 9A 10C |
| T/F | T F T T F T T T T T |
| Coding | Compare with subject PDF + your own main |
| Memory | Must match your actual implementation |
| Debug | Discuss in meeting — multiple valid answers |
